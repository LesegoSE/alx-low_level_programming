#include "main.h"
/**
 * print_alphabet - print all alphabet in lower case
 */
void print_alphabet(void)
{
	char l;

	for (letter = 'a'; letter <= 'z'; letter++)
	{
		_putchar(l);
	}

	_putchar('\n');
}
